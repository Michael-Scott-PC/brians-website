import axios from '../api/axiosStrapi';

import { GET_CURRENT_MORTGAGE_RATES } from './types';

export const getCurrentMortgageRates = () => async dispatch => {
  try {
  } catch (error) {
    console.log(error);
  }
};
